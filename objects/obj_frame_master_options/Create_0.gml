// Inherit the parent event
event_inherited();

// Set frame title
text = "Master Options"

// Set the frame title color
text_color = c_white

// Add frame elements
add_element(obj_button_master_volume_up)
add_element(obj_text_area_master_volume)
add_element(obj_button_master_volume_down)