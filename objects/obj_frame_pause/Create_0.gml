// Inherit the parent event
event_inherited();

// Set frame title
title = "Pause menu"

// Add frame elements
add_element(obj_button_back_pause)
add_element(obj_button_retry)
add_element(obj_button_quit)

// Center the frame position
set_frame_position()